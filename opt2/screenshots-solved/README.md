# Solved screenshot evidence

These screenshots correspond to Figures 2A–2C in `lab_instruction.pdf`.

- `figure-2a-compile-deploy.png`: successful compile and checker setup.
- `figure-2b-eth-htlc-result.png`: `runAllTests` with 3 wei and `isSolved == true`.
- `figure-2c-nft-htlc-result.png`: `runAllTests` with 1 wei and `isSolved == true`.

All addresses and balances are Remix VM test values only.
